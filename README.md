# Carry

Git carries your code. Carry carries the rest.

Carry is a Git-aware CLI for moving explicitly selected local, ignored repository state between clones and machines.

Carry only manages files that Git does not track and that Git is explicitly configured to ignore.

> Early development